cd Documents\ecommers
virt\Scripts\activate
cd ecom
python manage.py runserver


from django.contrib.sessions.models import Session

# Get session by key (note: use get() with pk=...)
try:
    session = Session.objects.get(pk='vlu7hhldq1pykok1g24ahjfa7eg6tkkn')
    session_data = session.get_decoded()
    print(session_data)  # This shows the decoded session data
except Session.DoesNotExist:
    print("Session not found")
except Exception as e:
    print(f"Error: {str(e)}")